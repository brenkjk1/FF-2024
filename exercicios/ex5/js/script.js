// function verificarNumero(val) {
//     return isNaN(valor);    
// }

// alert(`É uma letra?` ${verificarNumero(5)});

function verificarNumero(val) {
    return isNaN(valor);    
}

alert(`É uma letra?` ${verificarNumero(L)});